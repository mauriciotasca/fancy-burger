import { ActionTrackerDirective } from './action-tracker.directive';

describe('ActionTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
