import { ItemFilterCompletePipe } from './item-filter-complete.pipe';

describe('ItemFilterCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemFilterCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
